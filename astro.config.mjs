// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build
export default defineConfig({
  // Set this to the live domain once chosen (used for canonical URLs / sitemaps).
  site: 'https://oxfordhistorysociety.co.uk',
});
